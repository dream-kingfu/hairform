# Changelog

HAIRFORM 的重要功能、线上修复、模型策略和运行配置变更都会记录在这里。

## [Unreleased]

### Fixed

- H5 上传确认页改为按原始比例完整显示照片，竖图和横图不再为了铺满容器而裁掉头顶、发际线或两侧画面。
- 创建任务接口会区分“旧页面缺少当前授权版本”和“当前页面尚未完成两项确认”，网页过期时明确提示刷新，避免把版本不一致误报成普通创建失败。
- H5 不再单独显示“本地人脸模型暂未加载”的冗余说明；人脸、角度和构图的服务端复核状态仍正常保留。

## [0.6.1] - 2026-07-31

### Added

- H5 三款正向发型卡新增“真实发型灵感”，按最新版 `styleId` 匹配国内平台原帖；首批覆盖 15 款目录各 1 条已核验抖音原帖，并预留小红书官方规范链接支持。
- 新增最小化静态灵感目录、官方域名与规范路径白名单、失效隐藏、第三方跳转确认及目录完整性自动测试。
- H5 上传页接入在独立 Web Worker 中运行的 MediaPipe 人脸关键点检查，新增单人脸、正面角度、头部倾斜、画面裁切、构图、清晰度与光线检查；模型不可用时自动降级到浏览器基础检查或服务端复核。
- 上传确认拆分为“肖像授权”和“AI 处理同意”两项，服务端拒绝缺少确认的任务，并随任务保存授权版本与确认时间；页面明确 HAIRFORM 不将照片用于训练、任务最长保留 24 小时且支持立即删除。
- 下载 PNG 报告新增可见的 AI 内容说明和 `AI_Generated`、`Generator`、`Content_Type` 元数据。
- 新增真人预览六项固定质量指标、发布评分函数与授权样本验收说明，质检模型增加发际线、耳侧与背景交界的发丝边缘检查。
- 新增 P0 国内免费授权肖像来源清单，优先采用项目自愿参与者、北京公共数据有条件开放目录和数据堂助研申请，并列出首批 30 张的国内用户覆盖结构与免费授权前置条件。

### Changed

- 博主内容坚持“只链接、不搬图、不训练”：不保存图片地址、笔记正文、粉丝或评论数据，不参与视觉分析、咨询改建议、Image2、真人预览验收和 PNG 报告；小红书候选在无法可靠核验规范原帖时保持隐藏。
- 选款后的最终确认栏补充长度、刘海、分缝或发色明度，并明确确认后锁定建议、完成后生成整张高清报告。
- 总建议改成“看到什么 → 为什么推荐 → 日常代价”的顾问式表达，调整偏好后的总结也先复述用户真实生活习惯。
- P0 真人预览验收不再接受国外人脸数据库、公共图库、社交平台、学术人脸库或供应商网页样例作为授权样本。
- P0 数据路线由采购优先调整为零采购、非商业内部测试优先；免费科研数据在未取得生成式编辑和指定第三方模型处理许可前只能本地使用，不能上传 Image2。
- 本版本只发布 H5 网页、服务端、管理后台和公开文档；微信小程序保持 V0.5，不在本次发布范围内。

### Verified

- Lint、生产构建与 48 项 H5/后端自动化测试通过；灵感目录覆盖、非法链接隐藏、正向卡片隔离、外链安全属性、报告隔离和 600px 窄屏布局均有自动化检查。
- 完成桌面和 390px 手机宽度网页复核，无横向溢出或浏览器错误；未使用真人照片触发真实 AI 调用。
- V0.6.1 已作为 Sites 版本 25 发布到现有生产地址；线上首页与管理入口返回 200，未登录管理会话按预期返回 401，首页版本标记已更新为 `V0.6.1`。
- 对话修改建议与真人预览功能开关保持关闭；微信小程序未更新；本次发布未使用真人照片进行验收。
- “真实发型灵感”卡片仍需在获得授权的完整结果流程中补做桌面与 390px 人工复核。
- 已清除本轮临时下载的国外合成肖像，未提交给任何模型，也未写入公开仓库。
- 已核验北京公共数据开放平台、数据堂助研协议、CAS-PEAL 与 CASIA-FaceV5 的官方说明；尚未提交申请、下载真人数据或触发真实模型调用。

## [0.6.0] - 2026-07-31

### Added

- H5 结果页新增“沟通改建议”顾问区，支持自由输入、五类快捷偏好、重点追问、偏好复述、确认修改和保留原建议。
- 新增三组受鉴权保护的咨询接口；每轮最多两次沟通、每任务最多两次成功修改，并提供任务锁、输入长度限制和全站每日额度。
- 管理后台新增独立沟通模型配置，可在 GPT / Kie Terra 与 Qwen 之间选择，并查看连接状态、当日沟通次数和修改次数。
- 数据库保存任务级对话状态、模型快照、确认偏好和调用计数，随任务在 24 小时后或立即删除时清空。

### Changed

- 修改建议后保留脸型、发质、发量、发际线、肤色和照片警告等视觉事实，只替换通过固定目录校验的发型、发色与风格建议。
- 后续沟通请求不包含原始照片、图片地址或访问令牌；修改完成后旧报告失效并重新排版，最终真人预览仍需用户选款确认后由 Image2 生成。
- 建议文案结合用户确认的长度、刘海、打理投入、发色变化和风格偏好，表达更自然、更像真人顾问。
- 本版本仅更新 H5 网页、服务端与管理后台，微信小程序保持 V0.5。

### Verified

- Lint、生产构建与 38 项 H5/后端自动化测试通过。
- 对话功能默认关闭；公开部署仍需单独确认。

## [0.5.0] - 2026-07-30

### Added

- 网页与微信小程序新增明确的“先选后生成”交互：选中发型或发色不会立即调用 Image2，只有点击最后确认按钮才开始生成。
- 两款推荐发色纳入按需真人预览范围；单任务仍只锁定一款，最多两次 Image2 调用（第二次仅用于失败重试）。
- 推荐卡新增基于脸型、发质、发量、发际线与肤色倾向的自然语言说明，让建议更像发型顾问，而不是标签堆叠。
- 完整高清报告支持所选发型或发色真人效果，并继续由程序排版清晰的双语文字、建议、色卡和整体风格。
- 新增与 V0.5 页面一致的社交分享封面和 Open Graph / X 元数据。

### Fixed

- 修复网页在 `analysis_ready` 文字优先状态下显示生成按钮但点击被前端拦截的问题。
- 发型与发色使用同一套后端选择、异步生成、质检、失败重试和报告重排链路。

### Verified

- Lint、生产构建与 22 项 H5/后端自动化测试通过。
- 微信小程序 5 项自动化测试与脚本语法检查通过。

## [0.4.1] - 2026-07-30

### Documentation

- 新增 `PROJECT_CONTEXT.md` 项目全景与交接文档，集中记录当前线上状态、架构、模型路由、后台、API、数据安全、小程序、验证结果、完整版本日志与 Git 历史。
- 校正 README 中已经过时的“默认生成真人预览”和“生产只支持 Kie”描述。
- 将完整交接资料迁移为本机忽略文件 `PROJECT_CONTEXT_PRIVATE.md`，从公开仓库移除内部路径、配置状态和运维细节。
- 新增可公开阅读的 `PROJECT_OVERVIEW.md`，只保留产品能力、公共架构、本地开发、安全原则与版本入口。

### Fixed

- 增加管理员登录的安全错误分类日志，区分未配置、临时锁定与密码不匹配；不记录密码、会话令牌或原始 IP。
- 管理员密码哈希改用仅含安全字符的十六进制格式，并保留旧 Base64 格式读取兼容；PBKDF2 参数不变。
- Sites 运行环境将 PBKDF2 单次迭代上限限制为 100,000；管理密码哈希调整为平台支持的 100,000 次，并继续配合长随机密码、随机盐、登录限速与会话密钥保护。
- 完成根因定位后移除临时密码与哈希指纹诊断日志。

## [0.4.0] - 2026-07-30

### Added

- 新增文字优先任务策略 `text-first-v1`：一次视觉分析即可输出完整双语发型建议、发色色卡、理发师沟通卡和可下载报告，默认不调用 Image2。
- 新增 Kie Terra、Qwen3.6-Flash、GLM-4.6V-Flash 三家固定视觉识别供应商适配器；任务创建时保存供应商与模型快照。
- 新增独立密码管理后台 `/admin`，提供供应商连接测试、切换、真人预览开关、当日用量和审计记录。
- 新增管理员 PBKDF2-HMAC-SHA256 密码校验、HttpOnly 会话、CSRF、登录限速、密码版本失效和审计保护。
- 网页与微信小程序新增无真人预览报告与原图版理发师沟通卡；上传前压缩至约1280px、JPEG质量85%。

### Changed

- Kie 积分检查移动到可选真人预览请求前，纯文字分析不再被 Image2 积分安全线阻塞。
- 真人发型预览改为后台全站开关控制，默认关闭；开启后仍保持每任务最多两次 Image2 调用与 Luna/Terra 质检。
- Image2 失败只影响可选预览，不再使已完成的文字报告失效。
- 报告上传接口支持 `analysis_ready`，并允许真人预览完成后覆盖为含预览版本。

### Security

- API Key 仍只存放在 Sites 加密环境变量，管理接口不会返回密钥、密码哈希或会话令牌。
- 管理配置采用版本号原子更新，供应商切换要求密钥已配置且30分钟内健康检查通过。

### Verified

- TypeScript、Lint、生产构建与21项自动化测试通过。
- 微信开发者工具完成重新编译，“摄影 / 发型”双导航和发型原生页面可正常打开；摄影页原有云端授权查询仍可能出现超时。

## [0.3.1] - 2026-07-30

### Added

- 新增精确公网 IP 白名单；白名单访客不受每小时、每天的个人任务次数限制。
- 新增白名单解析测试，明确拒绝通配符和 CIDR 网段配置。

### Changed

- 白名单只绕过个人限流，继续保留全站任务、模型调用、单任务 Image2 次数和服务熔断保护。
- 生产环境已将当前测试电脑的公网 IP 作为保密配置加入白名单。

### Fixed

- Image2 改为异步任务提交与轮询，避免长时间同步等待被托管平台中断。
- 生成完成后继续执行 Luna 质检，并在必要时按单任务上限进行一次 Image2 重试。

### Verified

- 生产环境真实完成照片上传、Terra 分析、发型选择、Image2 单图生成和 Luna 质检。
- 实测单次 Image2 调用生成预览成功；测试任务、服务端照片和本地测试副本已删除。
