# HAIRFORM 真人预览质量验收口径

本文件定义真人发型/发色预览的固定验收口径。它用于比较模型或提示词版本，不用于训练模型，也不允许把其他网站用户照片直接纳入样本。

## 样本边界

- 第一阶段建立 100–300 张、逐张取得明确测试授权的正面肖像集；正式扩充前先完成不少于 30 张的小规模验收。P0 样本以中国大陆成年用户为主，不使用国外人脸数据库代替国内用户验收。
- 样本优先来自项目自行招募的中国大陆成年自愿参与者，或经专用协议批准的境内免费开放/助研数据。GitHub、ModelScope、公共图库、社交平台、学术人脸库和供应商网页样例均不能因为“免费”或“可下载”就视为已授权；科研许可也不能自动解释为允许第三方生成式模型处理。
- 每张样本记录授权来源、允许用途、到期时间和删除状态，不记录公开分享链接作为“授权”。授权范围必须明确包含生成式图像编辑评测、生成结果留存与人工评分；如果照片会由第三方模型处理，还要列明处理方、处理地点、保存期限及是否允许跨境传输。
- 覆盖不同发质、发量、脸型、发际线、肤色和常见拍摄条件；不采集身份、健康、种族等额外推断标签。
- 原始照片、生成结果和人工评分均放在受控存储中，按授权期限删除，不进入公开仓库。

国内免费授权图源的候选、申请前置条件和首批 30 张覆盖结构见 [P0_DOMESTIC_PORTRAIT_SOURCING.md](P0_DOMESTIC_PORTRAIT_SOURCING.md)。未取得包含生成式编辑和指定第三方处理权限的专用协议或参与者同意书前，不得把候选照片提交给任何模型。

## 六项指标

| 指标 | 通过标准 |
| --- | --- |
| 身份保持 `identity_preserved` | 五官、脸型和人物身份没有明显变化 |
| 发型目标匹配 `hair_target_matched` | 长度、刘海、分缝、轮廓或发色与选中建议一致 |
| 非头发区域保护 `non_hair_preserved` | 服装、背景、皮肤和配饰没有无关改动 |
| 伪影控制 `artifact_free` | 没有重复五官、断裂耳朵、异常手指或明显生成痕迹 |
| 发丝边缘 `hair_edge_quality` | 发际线、耳侧和背景交界自然，没有硬边与涂抹感 |
| 用户满意 `user_satisfaction` | 用户认为结果可用于决定或与理发师沟通 |

每项按 0–1 评分。前三项与伪影控制属于发布硬门槛，单项低于 0.8 即不通过；前五项均齐全且平均分不低于 0.8 才可进入候选版本。用户满意度在真实验收后补充，不允许用模型自评分代替。

## 发布报告

每次更换生成模型、质检模型、提示词或蒙版策略时，必须输出同一授权样本集的：

- 各指标平均值、P50、P95 和失败率。
- 按发质、发量、肤色与拍摄条件拆分的失败分布。
- 生成延迟、失败重试率和单张成本。
- 与当前线上版本的逐项差异，以及是否达到发布门槛。

任何样本授权不清、关键指标缺失或身份保持下降的版本都不得公开发布。
