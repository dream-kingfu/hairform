# HAIRFORM AI 协作约定

本文件是仓库内所有 AI 开发工具的公共最高约束。目标是让任何新电脑或新会话都能复现“GPT 决策、DeepSeek 开发、确定性工具验证、GPT 验收”的工作流，同时不公开主控电脑的私有运维信息。

## 事实来源

按以下顺序确认当前事实，后面的来源不得静默覆盖前面的来源：

1. 当前任务单与验收标准。
2. 当前分支的源代码、数据库迁移和测试。
3. 本文件与 `.ai-company/` 公共规则。
4. `README.md`、`PROJECT_OVERVIEW.md`、`CHANGELOG.md`。
5. 主控电脑上被 Git 忽略的 `AGENTS.private.md`、`PROJECT_CONTEXT_PRIVATE.md` 和私有 Skill；这些文件只能补充运维事实，不能降低公共安全要求。

发现来源冲突时停止猜测，在任务报告中列出冲突并交给 GPT 主控裁决。不得把私有文件内容复制到任务单、日志、提交、PR 或公开回复。

## 角色与代码权限

| 角色 | 默认职责 | 默认写入范围 |
|---|---|---|
| GPT 主控 | 理解需求、产品决策、架构、任务拆解、能力路由、安全审查、图片语义验收、最终验收 | `AGENTS.md`、`.ai-company/`、`.agents/skills/`、任务明确授权的公共文档 |
| DeepSeek 开发 | 实现和修复业务代码、数据库、测试与构建问题 | 任务单 `allowed_paths` 明确列出的文件 |
| 确定性工具 | Lint、构建、自动化测试、浏览器实测、GitHub Actions | 不作产品决策 |

- GPT 主控默认不修改 `app/`、`lib/`、`db/`、`drizzle/`、`tests/` 中的业务实现。只有任务单明确写出 `implementation_owner: gpt`，或用户明确要求 GPT/Codex 直接实现时才可例外。
- DeepSeek 只能执行 `owner: deepseek` 且包含验收标准、允许路径和禁止路径的任务。不得自行扩大功能范围或改写需求。
- 开发者和最终验收者不得只凭自述通过；必须同时有确定性检查结果。GPT 可以审核自己获准实现的紧急改动，但必须明确披露同一角色实现与复核的限制。
- “DeepSeek 开发角色”只描述代码执行者，不代表 HAIRFORM 线上运行时应接入 DeepSeek。产品模型白名单仍以 `lib/server/model-policy.ts` 为准。
- 默认最多返修两轮。两轮后仍不通过，输出阻塞原因、最小决策问题和保留完好的当前代码，不进行无限循环。

## 任务契约

业务代码修改前，GPT 主控应基于 `.ai-company/templates/task.md` 创建任务单。任务单至少包含：

- 用户可见结果与商业目标；
- 不做什么及保持不变的范围；
- `owner`、`implementation_owner`、`allowed_paths`、`forbidden_paths`；
- 可验证的验收标准与必跑命令；
- 隐私、模型、数据库和发布边界；
- 最大返修轮次。

不含敏感信息的任务单可放在 `.ai-company/tasks/<task-id>/task.md`。包含生产配置、账号、内部地址、真实照片位置或其他私有事实的任务只能保存在被忽略的私有目录中。

DeepSeek 完成后按 `.ai-company/templates/developer-report.json` 交付。GPT 最终按 `.ai-company/templates/review.json` 输出验收结果。报告必须反映真实命令结果，不得把“未运行”写成“通过”。

## 标准流程

1. GPT 主控把口语需求翻译成用户结果、非目标、风险与验收标准。
2. GPT 主控在任务单中选择代码执行者，并锁定允许修改范围。
3. DeepSeek 在独立分支或工作区内实现，补齐对应测试并提交开发报告。
4. 确定性工具运行 Lint、构建、测试和必要的浏览器检查。
5. GPT 对照原需求、任务单、`git diff`、测试证据和视觉证据验收。
6. 不通过时只生成针对阻塞项的返修任务；通过后再进入提交、PR 或发布阶段。

不得跳过任务契约直接让执行模型“读取整个仓库自行发挥”。小型、无歧义、低风险的文档修改可以在任务描述中内联同等信息，不必机械创建目录。

## HAIRFORM 产品不变量

- 保持 `text-first-v1`：先分析和展示文字建议，用户选择并最终确认后才允许 Image2。
- 视觉事实与可编辑建议分离。沟通可改发型、刘海、分缝、发色、风格和理由，不得改写脸型、发质、发量、发际线、额头比例、肤色倾向、照片质量与警告。
- 发型和发色必须来自固定目录；拒绝客户端传入模型名和白名单外 ID。
- 首次分析后的咨询请求只发送结构化分析、当前建议与短文本偏好，不再次发送原图、图片地址、任务令牌或隐藏提示词。
- 视觉分析、咨询、图片生成和质量检查保持独立能力路由；新任务创建时保存供应商和模型快照。
- 图片、密钥、任务令牌、完整提示词、原始 IP 和私有上下文不得进入普通日志、公开文档或提交。
- D1 与 R2 的 24 小时到期和立即删除必须同步覆盖原图、蒙版、预览、报告、咨询数据和任务状态。
- 不输出医学、植发、身份、吸引力、族裔或人格判断；不得将访问者照片用于训练或跨用户复用。

## 文件所有权

- H5：`app/HairApp.tsx`、`app/globals.css`。
- 管理后台：`app/admin/`、`app/api/admin/`。
- 服务端 API：`app/api/v1/`。
- 业务类型与展示：`lib/hair/`、`lib/client/`。
- 模型与任务处理：`lib/server/`。
- 数据库：`db/schema.ts`、`lib/server/jobs.ts`、`drizzle/`。
- 自动化测试：`tests/`。
- 公共 AI 协作层：`AGENTS.md`、`.agents/skills/`、`.ai-company/`、`.github/`。

微信小程序是独立发布面，默认不在本仓库任务范围内。除非任务明确包含小程序，否则不得修改其独立工程或声称已经同步。

## 修改与验证

- 修改前检查当前分支、`git status --short` 和相关历史；预先存在的改动都属于用户。
- 先搜索现有实现和测试，再增加抽象。数据库变更必须同步更新 Drizzle schema、运行时建表兼容逻辑、下一份手工迁移和测试。
- 新增状态、接口、模型约束、锁、删除规则或隐私边界时必须增加自动化覆盖。
- 常规完整门禁为：`npm run lint`、`npm test`。`npm test` 已包含生产构建。必要时追加窄屏浏览器检查，但不得为了 UI 测试上传个人照片。
- 使用本机 `maintain-hairform` Skill 时，最终运行其 `scripts/verify-hairform.ps1 -Mode full`。
- 产品、代码、UI、API、数据库、配置、版本或发布发生实质变化时更新 `CHANGELOG.md`；纯协作规则变更记录在 `Unreleased`，不自动提升产品版本。

## GitHub 与发布边界

- 分支、提交和 PR 只包含当前任务授权文件。不得使用笼统暂存把无关的用户文件带入提交。
- PR 必须说明用户影响、修改范围、验证证据、未验证项、隐私/模型/数据库/发布影响和返修轮次。
- GitHub Actions 是客观门禁，不替代 GPT 的产品与视觉验收。
- 推送公开 GitHub 前检查密钥、私有路径、内部配置、真实照片和本机运维信息。
- 推送 GitHub、修改生产配置、启用功能开关、调用真实供应商、使用真实照片、更新微信小程序和部署生产是相互独立的权限。任务只授权其中明确包含的动作。
