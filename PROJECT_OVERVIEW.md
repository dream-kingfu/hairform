# HAIRFORM 项目概览

HAIRFORM 是一个面向普通用户的响应式 H5 与微信原生小程序发型分析服务。用户上传一张获得授权的正面肖像后，可以获得结构化发型建议、发色色卡、理发师沟通卡和可下载的双语报告。

V0.6.3 的对话式修改建议、真实发型灵感和自适应上传预览当前仅提供给 H5 网页；微信原生小程序保持 V0.5 流程。

## 当前产品模式

默认采用 `text-first-v1` 文字优先策略：

- 一次视觉分析即可提供完整文字建议。
- 默认不生成真人发型预览，减少等待时间和图片生成成本。
- 真人预览可以由服务端开关启用。
- 网页用户对首次建议不满意时，可以先用文字告诉顾问想调整的长度、刘海、打理难度、风格或发色；确认偏好后再生成一版受目录约束的新建议。
- 启用后用户可从三款推荐发型或两款推荐发色中选择一款生成。
- 生成失败不会影响已经完成的文字报告。

## 主要功能

- 正面肖像上传，以及在浏览器独立线程中完成的清晰度、光线、单人脸、正面角度和构图检查；不可用时由服务端继续复核。
- 上传确认页以前景完整原图叠加同图模糊背景自动填满空白，不裁掉头顶、发际线或两侧画面，也不拉伸人物；网页版本过旧时会提示刷新。
- 脸型、发质、发量、发际线、额头比例、肤色和风格分析。
- 最佳短发、最佳中发、最佳长发和谨慎选择建议。
- 两款推荐发色色卡。
- 三款推荐发型对应的理发师沟通卡。
- 中文为主、英文为辅的固定短标签。
- `2160×3840` 高清报告和 `1080×1920` 页面预览。
- 报告可见 AI 内容说明和 PNG 机器可读元数据。
- 匿名反馈、系统分享、立即删除和 24 小时自动过期。
- 最多两轮沟通、两次确认修改；开始生成真人预览后锁定沟通入口。
- 三款正向发型按当前推荐 `styleId` 展示最多两条国内平台原帖灵感；谨慎选择、发色卡和下载报告不展示。

## 技术结构

- 前端：React、Next.js、TypeScript。
- 构建与运行：vinext、Vite、Cloudflare Workers 兼容运行时。
- 数据库：D1，保存任务状态、结构化分析和匿名运行数据。
- 对象存储：R2，保存原图、可选预览和报告。
- 微信端：原生微信小程序页面，不依赖 WebView。

服务端通过固定模型白名单提供视觉分析，并使用 JSON Schema 和固定发型目录校验模型结果。展示文案由程序双语字典生成，不直接采用模型自由文案。

沟通改建议使用独立模型配置，只在 Kie Terra（GPT）和 Qwen 中选择。后续请求仅包含首次结构化分析、当前建议和用户文字偏好，不再次发送照片、图片地址或访问令牌；视觉事实由服务端强制保留。

## 支持的视觉供应商

- Kie Terra：`gpt-5-6-terra`
- 阿里云千问：`qwen3.6-flash`
- 智谱 GLM：`glm-4.6v-flash`

可选真人预览使用 Kie Image2 图生图接口。前端不能传入或修改模型名称，服务端不会静默切换到白名单外模型。

## 理发师沟通卡

项目内置 15 款男士发型目录和固定理发参数。沟通卡包含：

- 顶部与前区目标长度。
- 两侧与后区处理方式。
- 刘海、分缝、层次与去量建议。
- 造型产品和避坑提醒。
- 发旋、生长方向、实际发量和打理时间的现场确认提示。

沟通卡由固定目录生成，不会增加 AI 调用。

## 安全与隐私

- API Key 只允许存放在未提交的本机环境文件或托管平台加密变量中。
- 图片、访问令牌和完整提示词不会写入业务日志。
- 任务使用高熵访问令牌保护，令牌不进入公开 URL。
- 原图、预览和报告最长保留 24 小时，并提供立即删除。
- 公网 IP 仅以加盐哈希用于限流，不保存原始地址。
- 管理接口使用独立密码、受保护会话、CSRF 和登录限速。
- 用户只能上传本人或已经获得授权的肖像。
- 肖像授权与 AI 处理分别确认，服务端保存任务级授权版本与确认时间并随任务清除；HAIRFORM 不将用户照片用于训练。

分析结果是基于单张照片的视觉建议，不是医学、植发或专业理发结论。

博主发型灵感是独立的 H5 静态外链目录，只保存平台、公开昵称、HAIRFORM 自写摘要、规范原帖 URL 和核验日期。页面不嵌入或请求抖音、小红书图片，不复制笔记正文，也不会把链接内容发送给分析模型、沟通模型或 Image2。外链仅作造型参考，不表示作者或平台与 HAIRFORM 合作或代言。

## 本地运行

要求 Node.js `>=22.13.0`。

```bash
npm install
npm run dev
```

复制 `.env.example` 为 `.env.local` 后填写本地测试需要的密钥。`.env.local` 不得提交到 GitHub。

未配置真实密钥时，可以使用 `DEMO_MODE=true` 验证页面流程；演示结果不能作为真实 AI 输出展示。

## AI 开发管理

项目采用轻量的双模型交接流程，不引入新的运行时 Agent 框架：

```text
GPT 需求判断、任务契约与架构
            ↓
DeepSeek 按允许路径实现业务代码
            ↓
Lint、构建、测试与必要的浏览器实测
            ↓
GPT 功能、视觉、隐私与发布验收
```

公共规则位于 `AGENTS.md`、`.agents/skills/` 与 `.ai-company/`。GitHub Actions 负责确定性质量门禁；是否推送、合并、修改生产配置、调用真实供应商、使用真人照片或部署生产仍分别授权。主控电脑的路径、账号、运行配置和私有运维方式不会进入公共协作文件。

## 验证

```bash
npm run lint
npm run build
npm test
```

自动化测试覆盖结构化分析、目录 ID、模型白名单、文字优先策略、理发师沟通卡、管理安全、限流和报告展示。

## 微信小程序

小程序底部为“摄影 / 发型”双导航：

- 摄影保留原有知识库、资料导入和权限逻辑。
- 发型为所有用户可用的微信原生页面。
- 支持照片选择、上传、进度、文字建议、报告、沟通卡、反馈和删除。
- 真人预览按钮由服务端开关控制。

正式发布前需要在微信公众平台配置服务器合法域名、隐私保护指引和对应服务类目，并完成 iOS 与 Android 真机验收。

## 版本与许可证

完整公开更新记录见 [CHANGELOG.md](CHANGELOG.md)。

项目使用 [GNU Affero General Public License v3.0](LICENSE)，许可证标识为 `AGPL-3.0-only`。
